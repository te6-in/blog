diff --git a/src/utils.ts b/src/utils.ts
index f4ba03fabadd9825656f621b08d935c9aad7f09f..b485ccc71c48a58851828d1193d729b11ff78ef2 100644
--- a/src/utils.ts
+++ b/src/utils.ts
@@ -29,17 +29,17 @@ export function renderPostAsHtml(post?: AppBskyFeedDefs.PostView | Post) {
 	let html = '';
 	for (const segment of rt.segments()) {
 		if (segment.isLink()) {
-			html += `<a href="${escapeHTML(segment.link?.uri)}">${escapeHTML(
+			html += `<a href="${escapeHTML(segment.link?.uri)}" target="_blank" rel="noopener noreferrer">${escapeHTML(
 				segment.text
 			)}</a>`;
 		} else if (segment.isMention()) {
 			html += `<a href="https://bsky.app/profile/${escapeHTML(
 				segment.mention?.did
-			)}">${escapeHTML(segment.text)}</a>`;
+			)}" target="_blank" rel="noopener noreferrer">${escapeHTML(segment.text)}</a>`;
 		} else if (segment.isTag()) {
 			html += `<a href="https://bsky.app/hashtag/${escapeHTML(
 				segment.tag?.tag
-			)}">#${escapeHTML(segment.tag?.tag)}</a>`;
+			)}" target="_blank" rel="noopener noreferrer">#${escapeHTML(segment.tag?.tag)}</a>`;
 		} else {
 			html += escapeHTML(segment.text);
 		}
